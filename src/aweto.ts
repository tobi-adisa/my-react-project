export type Student = {
    name: string;
    age: number;
    location: string;
}

export type numbers_and_strings = [number, number, number, number, string, string, string, number, number]
export type numbers = number[]